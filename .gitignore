# 1. すべてのファイルを一旦除外
*

# 2. 直下の指定ファイルのみ許可
!.gitignore
!audit.js
!index.js
!package.json
!package-lock.json
!仕様書.txt
!Modelfile

# 3. 指定フォルダ自体のトラックを許可
!log/
!kakuyomu_episodes/

# 4. フォルダ内の .gitkeep のみ許可（中身のファイルはすべて除外される）
!.gitkeep
